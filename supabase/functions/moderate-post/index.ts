import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
serve(async (req)=>{if(req.method!=="POST")return json({error:"Method not allowed"},405);const post=await req.json();const text=`${post.title||""}\n${post.content||""}`.toLowerCase();const spam=["casino","viagra","crypto pump","guaranteed income","wire transfer","forex"];const hit=spam.find(s=>text.includes(s));if(hit)return json({status:"hold",reason:`Possible spam signal: ${hit}`,summary:"This post needs review before publishing."});return json({status:"approved",reason:"No obvious spam signals detected.",summary:summarize(post.content||"")})});
function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{"Content-Type":"application/json"}})}
function summarize(c:string){const clean=c.replace(/\s+/g," ").trim();return clean.length>220?`${clean.slice(0,220)}...`:clean}
